<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Projet_Final.Afficher_Client"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Projet_Final"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
        <ListView x:Name="lvClients" SelectionChanged="lvClients_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Client">
                    <StackPanel Orientation="Vertical"
                                Margin="5" HorizontalAlignment="Stretch">
                        <TextBlock Text="{x:Bind Id}" Margin="5,5,5,10"/>
                        <TextBlock Text="{x:Bind Nom}" Margin="5,5,5,10"/>
                        <TextBlock Text="{x:Bind Adresse}" Margin="5,5,5,10"/>
                        <TextBlock Text="{x:Bind Num}" Margin="5,5,5,10"/>
                        <TextBlock Text="{x:Bind Email}" Margin="5,5,5,10"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        </StackPanel>
        <Button Click="Button_Click">Refresh</Button>

    </Grid>
</Page>
